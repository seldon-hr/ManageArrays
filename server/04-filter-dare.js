const array = ['amor', 'sol', 'piedra', 'día'];

// const array = words.filter(item => item.length >= 4);
// console.log('NEW ARRAY', array);

function solution (array) {
  return array.filter(item => item.length >= 4);
}

solution();
